<h1><%= @task.title %></h1>

<p><%= @task.description %></p>
<p><%= @task.state %></p>
<p><%= @task.deadline %></p>

<ul>
  <li><%= link_to "Edit", edit_task_path(@task) %></li>
  <li><%= link_to "Destroy", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
</ul>
<%= link_to 'Back', tasks_path %>
